[pytest]
markers =
    set_fake_event: populate the "stream events" Lichess API end point with a fake event
    set_fake_game_state: populate the "stream bot game state" Lichess API end point with a fake event